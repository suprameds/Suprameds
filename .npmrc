# Allow npm ci to tolerate peer dependency version mismatches.
# Needed because @medusajs/draft-order@2.13.4 declares peerOptional react@^18.3.1
# but the admin dashboard uses react@19.x (overridden at root level).
legacy-peer-deps=true
