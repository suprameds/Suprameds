<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTPS-only. No cleartext traffic.
  system + user CA trust anchors (user certs disabled on release, so QA proxying
  for debugging still works but release builds don't trust user-installed certs).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
